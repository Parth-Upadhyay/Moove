<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#F5F5F5">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Exercise"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="32dp" />

    <RadioGroup
        android:id="@+id/sideSelection"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="32dp">

        <RadioButton
            android:id="@+id/radioLeft"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Left Side"
            android:paddingEnd="16dp" />

        <RadioButton
            android:id="@+id/radioRight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Right Side"
            android:checked="true" />
    </RadioGroup>

    <Button
        android:id="@+id/btnPendulum"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Pendulum"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnCrossover"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Crossover Stretch"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnExternalRotation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="External Rotation"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnWallClimb"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Wall Climb" />

</LinearLayout>